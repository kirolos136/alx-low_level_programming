#include <stdio.h>

/**
 * main - entry point
 *
 * description : prints
 *
 * Return:0 always for succes operation
 */


int main(void)
{
	int n = 0;

	while (n < 10)
	{
		printf("%d", n);
		n++;
	}
	printf("\n");
	return (0);
}
