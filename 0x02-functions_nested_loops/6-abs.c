#include "main.h"
/**
 * _abs - entry point
 * @n : entered integer
 * description : prints the absolue value
 *
 * Return:0 always for succes operation
 */
int _abs(int n)
{
	if (n < 0)
	{
		n = n * -1;
		return (n);
	}
	else
	{
		return (n);
	}
}
