#include "main.h"
#include <stdio.h>
/**
 * print_rev - check the code
 * @s : pointer to character
 * description : reverse a string
 * Return: nothing.
 */
void print_rev(char *s)
{
	int i = 0;

	while (s[i])
		i++;
	while (i--)
		_putchar(s[i]);
	_putchar('\n');	
}
