#include "main.h"
#include <stdio.h>
/**
 * _puts - check the code
 * @str : takes a pointer to character
 * description :prints a string, followed by a new line
 * Return: nothing
 */

void _puts(char *str)
{
	printf("%s\n", str);
}
